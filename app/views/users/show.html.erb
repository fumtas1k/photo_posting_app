<% provide(:title, "profile") %>
<div class="profile-wrapper">
  <h1><%= get_user_name(@user) %></h1>
  <p><%= @user.email %></p>
  <%= link_to "退会", user_path(@user.id), method: :delete, data: {confirm: "本当に退会しますか?"}, class: "btn btn-danger" %>
</div>
